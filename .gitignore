# Written by Noctalia at runtime — per-machine settings values for this plugin.
settings.json

# Editor / backup noise
*.bak
*.bak-*
*~
*.swp
.vscode/
.idea/
.qmlls.ini

# Byte-compiled Python (the cache helper lives outside this repo, but a local
# copy or a py_compile run drops these)
__pycache__/
*.pyc

# Local scratch
/tmp/
*.log
